% General Letter LaTeX Template
\documentclass[11pt,letterpaper]{article}

\usepackage{ifxetex}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{geometry}
\usepackage{hyperref}
\usepackage{microtype}
\usepackage{parskip}
\usepackage{xcolor}

\ifxetex
  \usepackage{fontspec}
  \setmainfont{FreeSans}[Ligatures=TeX]
  \setsansfont{FreeSans}[Ligatures=TeX]
\else
  \usepackage{helvet}
  \renewcommand{\familydefault}{\sfdefault}
\fi

\geometry{
  letterpaper,
  left=1.25in,
  right=1.25in,
  top=1.0in,
  bottom=1.0in
}

% Midnight Galaxy palette
\definecolor{headingcolor}{RGB}{43,30,62}    % #2b1e3e Deep Purple
\definecolor{rulecolor}{RGB}{164,144,194}    % #a490c2 Lavender
\definecolor{mutedcolor}{RGB}{120,106,150}   % muted lavender

% ── Contact separator ─────────────────────────────────────────────────────
\newcommand{\sep}{\enspace{\color{rulecolor}\textbullet}\enspace}

\hypersetup{
  colorlinks=false,
  hidelinks,
  pdfborder={0 0 0},
  pdftitle={Letter — $if(author)$$author$$endif$},
  pdfauthor={$if(author)$$author$$endif$}
}

\setlength{\parindent}{0pt}
\setlength{\parskip}{0.9em}
\linespread{1.2}
\widowpenalty=10000
\clubpenalty=10000
\pagestyle{empty}

\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% ══════════════════════════════════════════════════════════════════════════
\begin{document}

% ── Sender header ─────────────────────────────────────────────────────────
\begin{flushleft}
  {\large\bfseries\color{headingcolor} $author$}\\[0.15em]
  {\small\color{mutedcolor}
    $if(address)$$address$$else$$if(location)$$location$$endif$$endif$%
    $if(email)$\sep\href{mailto:$email$}{$email$}$endif$%
    $if(phone)$\sep$phone$$endif$%
  }
\end{flushleft}

\vspace{0.3em}
{\color{rulecolor}\hrule height 0.5pt}
\vspace{1.2em}

% ── Date ──────────────────────────────────────────────────────────────────
$if(date)$
$date$

$endif$

% ── Recipient address block ───────────────────────────────────────────────
$if(recipient)$
$recipient$$if(recipient-title)$\\$recipient-title$$endif$$if(recipient-company)$\\$recipient-company$$endif$$if(recipient-address)$\\$recipient-address$$endif$

$endif$

% ── Subject line ──────────────────────────────────────────────────────────
$if(subject)$
\textbf{Subject: $subject$}

$endif$

% ── Body ──────────────────────────────────────────────────────────────────
$body$

% ── Closing ───────────────────────────────────────────────────────────────
\vspace{1.5em}
$if(closing)$$closing$$else$Sincerely$endif$,

\vspace{2.5em}
$author$

\end{document}

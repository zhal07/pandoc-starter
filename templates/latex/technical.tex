% Technical Documentation LaTeX Template
% Versioned, TOC, numbered sections, syntax highlighting, admonition blocks
\documentclass[$if(fontsize)$$fontsize$$else$11pt$endif$,$if(papersize)$$papersize$paper$else$letterpaper$endif$]{article}

\usepackage{ifxetex}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{geometry}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{calc}
\usepackage{array}
\usepackage{microtype}
\usepackage{fancyhdr}
\usepackage{lastpage}
\usepackage{parskip}
\usepackage{float}
\usepackage{caption}
\usepackage{tocloft}
\usepackage{tcolorbox}
\usepackage{mdframed}
\usepackage{setspace}

\tcbuselibrary{skins, breakable}

\ifxetex
  \usepackage{fontspec}
  \setmainfont{FreeSans}[Ligatures=TeX]
  \setsansfont{FreeSans}[Ligatures=TeX]
  \setmonofont{FreeMono}[Scale=0.9]
\else
  \usepackage{helvet}
  \renewcommand{\familydefault}{\sfdefault}
\fi

\geometry{
  left=1.0in,
  right=1.0in,
  top=1.0in,
  bottom=1.0in,
  headheight=14pt
}

% ── Color palette — Midnight Galaxy ───────────────────────────────────────
\definecolor{headingcolor}{RGB}{43,30,62}    % #2b1e3e Deep Purple
\definecolor{accentcolor}{RGB}{74,78,143}    % #4a4e8f Cosmic Blue
\definecolor{rulecolor}{RGB}{164,144,194}    % #a490c2 Lavender
\definecolor{linkcolor}{RGB}{74,78,143}      % #4a4e8f Cosmic Blue
\definecolor{codebg}{RGB}{245,243,252}       % light silver-purple tint
\definecolor{notecolor}{RGB}{74,78,143}      % #4a4e8f Cosmic Blue
\definecolor{notebg}{RGB}{230,230,250}       % #e6e6fa Silver
\definecolor{warncolor}{RGB}{160,80,0}       % amber (semantic, unchanged)
\definecolor{warnbg}{RGB}{255,247,235}       % amber tint (semantic, unchanged)
\definecolor{tipcolor}{RGB}{0,120,70}        % green (semantic, unchanged)
\definecolor{tipbg}{RGB}{235,252,244}        % green tint (semantic, unchanged)

% ── Meta separator ────────────────────────────────────────────────────────
\newcommand{\sep}{\enspace{\color{rulecolor}\textbullet}\enspace}

% ── Admonition boxes (used by admonition.lua filter) ──────────────────────
\newtcolorbox{notebox}{
  breakable, enhanced,
  colback=notebg, colframe=notecolor,
  leftrule=3pt, toprule=0pt, bottomrule=0pt, rightrule=0pt,
  left=0.8em, right=0.8em, top=0.5em, bottom=0.5em,
  fonttitle=\small\bfseries\color{notecolor},
  title=NOTE
}

\newtcolorbox{warningbox}{
  breakable, enhanced,
  colback=warnbg, colframe=warncolor,
  leftrule=3pt, toprule=0pt, bottomrule=0pt, rightrule=0pt,
  left=0.8em, right=0.8em, top=0.5em, bottom=0.5em,
  fonttitle=\small\bfseries\color{warncolor},
  title=WARNING
}

\newtcolorbox{tipbox}{
  breakable, enhanced,
  colback=tipbg, colframe=tipcolor,
  leftrule=3pt, toprule=0pt, bottomrule=0pt, rightrule=0pt,
  left=0.8em, right=0.8em, top=0.5em, bottom=0.5em,
  fonttitle=\small\bfseries\color{tipcolor},
  title=TIP
}

% ── Hyperlinks ────────────────────────────────────────────────────────────
\hypersetup{
  colorlinks=true,
  linkcolor=linkcolor,
  urlcolor=linkcolor,
  citecolor=linkcolor,
  pdfborder={0 0 0},
  pdftitle={$if(title)$$title$$if(version)$ v$version$$endif$$endif$},
  pdfauthor={$if(author)$$author$$endif$}
}

% ── Section headings ──────────────────────────────────────────────────────
$if(numbersections)$
\titleformat{\section}
  {\large\bfseries\sffamily\color{headingcolor}}
  {\thesection\ }{0em}{}
  [\vspace{-0.3em}{\color{rulecolor}\hrule height 0.5pt}\vspace{0.2em}]

\titleformat{\subsection}
  {\normalsize\bfseries\sffamily\color{headingcolor}}
  {\thesubsection\ }{0em}{}

\titleformat{\subsubsection}
  {\normalsize\sffamily\color{headingcolor}}
  {\thesubsubsection\ }{0em}{}
$else$
\titleformat{\section}
  {\large\bfseries\sffamily\color{headingcolor}}
  {}{0em}{}
  [\vspace{-0.3em}{\color{rulecolor}\hrule height 0.5pt}\vspace{0.2em}]

\titleformat{\subsection}
  {\normalsize\bfseries\sffamily\color{headingcolor}}
  {}{0em}{}

\titleformat{\subsubsection}
  {\normalsize\sffamily\color{headingcolor}}
  {}{0em}{}

\setcounter{secnumdepth}{0}
$endif$

\titlespacing*{\section}{0pt}{1.5em}{0.6em}
\titlespacing*{\subsection}{0pt}{1.0em}{0.3em}
\titlespacing*{\subsubsection}{0pt}{0.6em}{0.2em}

% ── Lists ─────────────────────────────────────────────────────────────────
\setlist{nosep, topsep=0.3em, itemsep=0.3em}
\setlist[itemize]{leftmargin=1.5em}
\setlist[enumerate]{leftmargin=1.5em}
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% ── Code highlighting ──────────────────────────────────────────────────────
$if(highlighting-macros)$
$highlighting-macros$
$endif$

% Code block background
\let\Shaded\relax
\let\endShaded\relax
\newenvironment{Shaded}{\begin{mdframed}[backgroundcolor=codebg,linewidth=0.5pt,linecolor=rulecolor,innerleftmargin=0.8em,innerrightmargin=0.8em,innertopmargin=0.5em,innerbottommargin=0.5em]}{\end{mdframed}}

% ── Tables ────────────────────────────────────────────────────────────────
\renewcommand{\arraystretch}{1.3}
\captionsetup{font=small, labelfont=bf}

% ── Paragraph style ───────────────────────────────────────────────────────
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.65em}
\linespread{1.15}
\widowpenalty=10000
\clubpenalty=10000

% ── Header / footer ───────────────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.4pt}
\fancyhead[L]{\small\sffamily\color{headingcolor} $if(title)$$title$$endif$$if(version)$ \textbf{v$version$}$endif$}
\fancyhead[R]{\small\sffamily\color{headingcolor} $if(author)$$author$$endif$}
\fancyfoot[C]{\small Page \thepage\ of \pageref{LastPage}}

\fancypagestyle{plain}{%
  \fancyhf{}
  \renewcommand{\headrulewidth}{0pt}
  \fancyfoot[C]{\small\thepage}
}

% ── TOC style ─────────────────────────────────────────────────────────────
\renewcommand{\cftsecfont}{\sffamily\bfseries}
\setlength{\cftbeforesecskip}{0.4em}
\setlength{\cftbeforesubsecskip}{0.15em}

% ── Footnotes ─────────────────────────────────────────────────────────────
\usepackage[hang, flushmargin]{footmisc}

% ══════════════════════════════════════════════════════════════════════════
\begin{document}
\thispagestyle{plain}

% ── Title block ───────────────────────────────────────────────────────────
$if(title)$
\begin{flushleft}
  {\LARGE\bfseries\sffamily\color{headingcolor} $title$}\\[0.3em]
  $if(subtitle)${\large\itshape $subtitle$}\\[0.3em]$endif$
  {\small\sffamily
    $if(author)$$author$$endif$%
    $if(author)$$if(date)$\sep$endif$$endif$%
    $if(date)$$date$$endif$%
    $if(version)$\sep\textbf{Version $version$}$endif$%
  }
\end{flushleft}
\vspace{0.3em}
{\color{accentcolor}\hrule height 1.5pt}
\vspace{0.05em}
{\color{rulecolor}\hrule height 0.5pt}
\vspace{0.8em}
$endif$

% ── Abstract ──────────────────────────────────────────────────────────────
$if(abstract)$
\begin{quote}
  \small\itshape $abstract$
\end{quote}
\vspace{0.5em}
$endif$

% ── Table of contents ─────────────────────────────────────────────────────
$if(toc)$
\tableofcontents
\vspace{1em}
\noindent{\color{rulecolor}\hrule height 0.4pt}
\vspace{1em}
$endif$

% ── Body ──────────────────────────────────────────────────────────────────
$body$

\end{document}

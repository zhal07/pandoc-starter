% Meeting Notes LaTeX Template
% Lightweight, structured layout for quick capture and distribution
\documentclass[11pt,letterpaper]{article}

\usepackage{ifxetex}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{geometry}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{xcolor}
\usepackage{microtype}
\usepackage{parskip}
\usepackage{fancyhdr}
\usepackage{array}
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{amssymb}

\ifxetex
  \usepackage{fontspec}
  \setmainfont{FreeSans}[Ligatures=TeX]
  \setsansfont{FreeSans}[Ligatures=TeX]
\else
  \usepackage{helvet}
  \renewcommand{\familydefault}{\sfdefault}
\fi

\geometry{
  letterpaper,
  left=1.0in,
  right=1.0in,
  top=0.9in,
  bottom=0.9in,
  headheight=14pt
}

% Midnight Galaxy palette
\definecolor{headercolor}{RGB}{43,30,62}     % #2b1e3e Deep Purple
\definecolor{accentcolor}{RGB}{74,78,143}    % #4a4e8f Cosmic Blue
\definecolor{rulecolor}{RGB}{164,144,194}    % #a490c2 Lavender
\definecolor{metabg}{RGB}{245,243,252}       % light silver-purple tint

\hypersetup{
  colorlinks=false,
  hidelinks,
  pdfborder={0 0 0},
  pdftitle={$if(title)$$title$$endif$},
  pdfauthor={$if(author)$$author$$endif$}
}

% ── Sections ──────────────────────────────────────────────────────────────
\titleformat{\section}
  {\normalsize\bfseries\sffamily\color{accentcolor}}
  {}{0em}{\MakeUppercase}
  [\vspace{-0.3em}{\color{rulecolor}\hrule height 0.5pt}\vspace{0.3em}]

\titleformat{\subsection}
  {\normalsize\bfseries\color{headercolor}}
  {}{0em}{}

\titlespacing*{\section}{0pt}{1.2em}{0.5em}
\titlespacing*{\subsection}{0pt}{0.8em}{0.3em}

% ── Lists ─────────────────────────────────────────────────────────────────
\setlist{nosep, topsep=0.2em, itemsep=0.2em}
\setlist[itemize]{leftmargin=1.5em}
\setlist[enumerate]{leftmargin=1.5em}
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% ── Paragraph style ───────────────────────────────────────────────────────
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.5em}
\linespread{1.1}
\widowpenalty=10000
\clubpenalty=10000

% ── Header / footer ───────────────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.3pt}
\fancyhead[L]{\small\sffamily\color{headercolor}\textbf{Meeting Notes}}
\fancyhead[R]{\small\sffamily\color{headercolor} $if(date)$$date$$endif$}
\fancyfoot[C]{\small\thepage}

\fancypagestyle{plain}{%
  \fancyhf{}
  \renewcommand{\headrulewidth}{0pt}
  \fancyfoot[C]{\small\thepage}
}

% ══════════════════════════════════════════════════════════════════════════
\begin{document}
\thispagestyle{plain}

% ── Meeting header ────────────────────────────────────────────────────────
\noindent
{\LARGE\bfseries\color{headercolor} $if(title)$$title$$else$Meeting Notes$endif$}

\vspace{0.4em}
{\color{rulecolor}\hrule height 1pt}
\vspace{0.6em}

% ── Meeting metadata table ────────────────────────────────────────────────
\noindent
\begin{tabularx}{\textwidth}{@{} l X @{}}
  $if(date)$\textbf{Date} & $date$ \\[0.3em]$endif$
  $if(location)$\textbf{Location} & $location$ \\[0.3em]$endif$
  $if(attendees)$\textbf{Attendees} & $for(attendees)$$attendees$$sep$,\ $endfor$ \\[0.3em]$endif$
\end{tabularx}

\vspace{0.5em}
{\color{rulecolor}\hrule height 0.3pt}
\vspace{0.8em}

% ── Body ──────────────────────────────────────────────────────────────────
$body$

\end{document}

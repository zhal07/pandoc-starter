% Article / Essay LaTeX Template
% Inline title block, wide readable margins, footnote support
\documentclass[$if(fontsize)$$fontsize$$else$12pt$endif$,$if(papersize)$$papersize$paper$else$letterpaper$endif$]{article}

\usepackage{ifxetex}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{geometry}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{calc}
\usepackage{array}
\usepackage{microtype}
\usepackage{fancyhdr}
\usepackage{lastpage}
\usepackage{parskip}
\usepackage{float}
\usepackage{caption}
\usepackage[hang, flushmargin]{footmisc}

\ifxetex
  \usepackage{fontspec}
  \setmainfont{FreeSans}[Ligatures=TeX]
  \setsansfont{FreeSans}[Ligatures=TeX]
  \setmonofont{FreeMono}[Scale=0.9]
\else
  \usepackage{helvet}
  \renewcommand{\familydefault}{\sfdefault}
\fi

% Wider inner margins for comfortable reading
\geometry{
  left=1.5in,
  right=1.5in,
  top=1.25in,
  bottom=1.25in,
  headheight=14pt
}

% Midnight Galaxy palette
\definecolor{headingcolor}{RGB}{43,30,62}    % #2b1e3e Deep Purple
\definecolor{rulecolor}{RGB}{164,144,194}    % #a490c2 Lavender
\definecolor{linkcolor}{RGB}{74,78,143}      % #4a4e8f Cosmic Blue
\definecolor{mutedcolor}{RGB}{120,106,150}   % muted lavender

% ── Meta separator ────────────────────────────────────────────────────────
\newcommand{\sep}{\enspace{\color{rulecolor}\textbullet}\enspace}

\hypersetup{
  colorlinks=true,
  linkcolor=linkcolor,
  urlcolor=linkcolor,
  citecolor=linkcolor,
  pdfborder={0 0 0},
  pdftitle={$if(title)$$title$$endif$},
  pdfauthor={$if(author)$$author$$endif$}
}

% ── Section headings ──────────────────────────────────────────────────────
\titleformat{\section}
  {\large\bfseries\color{headingcolor}}
  {$if(numbersections)$\thesection.\ $endif$}{0em}{}

\titleformat{\subsection}
  {\normalsize\bfseries\color{headingcolor}}
  {$if(numbersections)$\thesubsection.\ $endif$}{0em}{}

\titleformat{\subsubsection}
  {\normalsize\itshape}
  {$if(numbersections)$\thesubsubsection.\ $endif$}{0em}{}

\titlespacing*{\section}{0pt}{1.5em}{0.5em}
\titlespacing*{\subsection}{0pt}{1.0em}{0.3em}

% ── Lists ─────────────────────────────────────────────────────────────────
\setlist{nosep, topsep=0.3em, itemsep=0.3em}
\setlist[itemize]{leftmargin=1.5em}
\setlist[enumerate]{leftmargin=1.5em}
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% ── Code blocks ───────────────────────────────────────────────────────────
$if(highlighting-macros)$
$highlighting-macros$
$endif$

% ── Tables ────────────────────────────────────────────────────────────────
\renewcommand{\arraystretch}{1.3}
\captionsetup{font=small, labelfont=bf}

% ── Paragraph style ───────────────────────────────────────────────────────
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.8em}
\linespread{1.2}
\widowpenalty=10000
\clubpenalty=10000

% ── Header / footer ───────────────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.4pt}
\fancyhead[L]{\small\itshape $if(title)$$title$$endif$}
\fancyhead[R]{\small $if(author)$$author$$endif$}
\fancyfoot[C]{\small\thepage}

\fancypagestyle{plain}{%
  \fancyhf{}
  \renewcommand{\headrulewidth}{0pt}
  \fancyfoot[C]{\small\thepage}
}

% ══════════════════════════════════════════════════════════════════════════
\begin{document}
\thispagestyle{plain}

% ── Inline title block ────────────────────────────────────────────────────
$if(title)$
\begin{center}
  {\LARGE\bfseries\color{headingcolor} $title$}\\[0.5em]
  $if(subtitle)${\large\itshape $subtitle$}\\[0.5em]$endif$
  {\normalsize\color{mutedcolor}
    $if(author)$$author$$endif$%
    $if(author)$$if(date)$\sep$endif$$endif$%
    $if(date)$$date$$endif$%
  }
\end{center}

\vspace{0.3em}
{\color{rulecolor}\hrule height 0.75pt}
\vspace{1em}
$endif$

% ── Abstract ──────────────────────────────────────────────────────────────
$if(abstract)$
\begin{quote}
  \small\itshape\color{mutedcolor} $abstract$
\end{quote}
\vspace{0.7em}
{\color{rulecolor}\hrule height 0.3pt}
\vspace{1em}
$endif$

% ── Table of contents ─────────────────────────────────────────────────────
$if(toc)$
\tableofcontents
\vspace{1em}
\noindent{\color{rulecolor}\hrule height 0.3pt}
\vspace{1em}
$endif$

% ── Body ──────────────────────────────────────────────────────────────────
$body$

\end{document}
